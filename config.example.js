// Copy this file to `config.js` (already done in the repo) and fill in your keys.
// You can also edit `config.js` directly — it's NOT gitignored anymore so it ships to Vercel.
//
// Get keys here:
//   GEMINI:    https://aistudio.google.com  (restrict the key to your Vercel domain!)
//   NEWSDATA:  https://newsdata.io/         (free tier: 200 credits/day)
//   GNEWS:     https://gnews.io/            (free tier: 100 calls/day, optional)
//   NYT:       https://developer.nytimes.com (free, optional)

export const CONFIG = {
  GEMINI_API_KEY: '',
  GEMINI_MODEL: 'gemini-2.5-flash',
  NEWSDATA_API_KEY: '',
  GNEWS_API_KEY: '',
  NYT_API_KEY: '',
  DEFAULT_PLACE: 'Kodaikanal',
};
